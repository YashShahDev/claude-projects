# hello-world

A minimal Rust CLI that prints `Hello, world!` to stdout.

Assumes a `Cargo.toml` at this project's root.

## Usage

```sh
make build PROJECT=hello-world
./projects/hello-world/target/debug/hello-world
```
